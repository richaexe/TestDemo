# Database administrative login by Unix domain socket
local   all             qaipadmin                              trust
local   all             postgres                               md5  # Require password for security

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                    md5  # Require password for other users

# IPv4 local connections:
host    all             all             127.0.0.1/32           md5  # Require password

# IPv6 local connections:
host    all             all             ::1/128                scram-sha-256  # Stronger authentication

# Allow replication connections from localhost, by a user with the replication privilege.
local   replication     all                                    md5
host    replication     all             127.0.0.1/32           scram-sha-256
host    replication     all             ::1/128                scram-sha-256
